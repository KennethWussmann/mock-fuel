language: java
jdk: openjdk9
sudo: false
after_success:
  - bash <(curl -s https://codecov.io/bash)

deploy:
  provider: script
  script: "./gradlew -Prelease publish"
  skip_cleanup: true
  on:
    tags: true